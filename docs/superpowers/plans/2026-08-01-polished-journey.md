# MiniDist Polished Journey Implementation Plan

## Task 1: Freeze extraction and parity contracts

- Add `journey/manifest.toml` with ten boundaries and owned roots/files.
- Add a deterministic history extractor that constructs each cumulative tree
  from the recorded file slices and real historical diffs.
- Test exact patch coverage, clean application, focused test presence, and final
  parity with the current repository.

## Task 2: Author Stages 01–05

- Write bilingual goals and layouts for simulation, shared protocol vocabulary,
  asynchronous replication, Raft, and experiments 1–3.
- Generate canonical patches and cumulative focused checks.
- Require every changed file to be owned exactly once.

## Task 3: Author Stages 06–10

- Preserve the real fencing delta in Stage 06.
- Isolate shared M3/failure/election changes in Stage 07.
- Add WAL shipping, ISR, and experiments 4–7 in separate causal Stages.
- Prove final byte parity with current owned files.

## Task 4: Render bilingual browser lessons

- Adapt the MiniRedis renderer to ten MiniDist Stages.
- Nest failure preview inside Test contract.
- Support grouped multi-file explanations and collapsed supporting files.
- Generate English and Chinese indexes plus ten pages per language.

## Task 5: Add direct Agent-guided entry

- Add resumable Stage-specific learner workspaces to `build_journey.py`.
- Add root `AGENTS.md` and short bilingual usage guides.
- Verify `READY`, `RESUME`, protected reset, no branch switch, cumulative tests,
  and canonical Stage parity.

## Task 6: Integrate navigation, localization, and CI

- Expose all three learning modes in README, homepage, and MkDocs nav.
- Preserve the current Journey path when switching languages.
- Add CI for generated-page drift, tooling tests, the ten-Stage chain, and strict
  documentation build.

## Task 7: Polished acceptance

- Run the full repository suite and compileall.
- Rebuild and test all ten Stages and prove final owned-tree parity.
- Build MkDocs strictly.
- Exercise representative bilingual Stage pages, collapsed drawers, ordering,
  language switching, and Agent routing in a real browser.
- Leave the feature worktree clean with local commits; do not push without
  explicit authorization.
